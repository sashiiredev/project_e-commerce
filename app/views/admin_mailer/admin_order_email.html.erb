<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>An user ordered something !</h1>
    <p><%= @user.email %> ordered the following items :</p>
    <table>
     <tr>
      <th>Image</th>
      <th>Title</th>
    </tr>
      <% @order.item_id.each do |order| %>
       <tr>
        <td></td>
        <td><%= order.item.title %></td>
        <td><%= order.item.description %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
